@startuml
skinparam monochrome true
skinparam shadowing false
skinparam dpi 220
skinparam defaultFontName "PingFang TC"
skinparam defaultFontSize 13
skinparam sequenceMessageAlign center
skinparam ParticipantPadding 18
hide footbox

actor 管理員
participant UI
participant API
database DB

activate 管理員
activate UI
== 記錄後台操作 ==
管理員 -> UI : 執行後台操作（例如停權會員）
UI -> API : 呼叫對應的後台 API
activate API
API -> DB : 更新資料
API -> DB : 寫入操作紀錄\n（摘要、欄位前後差異、還原資料）
API --> UI : 200 OK
deactivate API

== 查詢操作紀錄 ==
管理員 -> UI : 開啟「操作紀錄」並篩選
UI -> API : GET /api/admin/operation-logs
activate API
API -> DB : 依對象類型、管理員與關鍵字查詢
API --> UI : 紀錄清單（每頁 50 筆）
deactivate API

== 一鍵復原 ==
管理員 -> UI : 點擊「復原」並確認
UI --> 管理員 : 要求重新驗證身分
管理員 -> UI : 輸入密碼或使用通行密鑰
UI -> API : POST /api/admin/operation-logs/:id/undo
activate API
API -> DB : 讀取紀錄，檢查權限\n與資料之後是否再被修改
API -> DB : 欄位還原為紀錄中的原值\n（錢包以反向調整還原）
API -> DB : 新增「還原」紀錄，\n原紀錄標示為已還原
API --> UI : 200 已還原（無權限、涉及金流、\n已還原或資料已被修改時回應 403／409）
deactivate API
UI --> 管理員 : 顯示處理結果
deactivate UI
deactivate 管理員
@enduml
